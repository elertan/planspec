Let's simulate using the brainstorm skill in skills/brainstorm/SKILL.md
Image you have this as a skill, walk to me through an example flow when the user prompts the following:
```
Instead of fetching the ride information from the API functions via `getByRides()` in Mendrix, let's just scrape their web page. Mendrix has stated they will not give us access to their API
  because it also has information on other parties
```